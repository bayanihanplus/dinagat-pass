# Dinagat Pass Web

Next.js App Router shell.

Current lane:

DINAGAT-PASS-MONOREPO-SCAFFOLD-01

This is not a traveler UI build.

Allowed in this shell:

- package setup
- TypeScript config
- health endpoint

Not allowed yet:

- traveler pages
- operator pages
- admin pages
- payment UX
- visit stamp UX
- QR scan UX
- marketplace UI

Hard rule:

The frontend displays backend truth. It does not create operational truth.
